# 2026-07-22 — Workspace index added to AGENTS.md

## Goal
Implement full workspace index/tree in `AGENTS.md` so any AI agent working here can quickly locate files/folders and their relationships without exploring the filesystem.

## What changed
- `AGENTS.md`: added `## Workspace Index` section with comprehensive tree covering:
  - Top-level files (`AGENTS.md`, `KNOWLEDGE.md`, `README.md`)
  - `knowledge/`, `notes/`
  - `code/` with full `dpa_kt/` package layout, configs, data_cache, datasets, notebooks, runs-50/200-epochs, scripts, venv
  - `claude-science/`, `sources/`, `working/`, `skills/`
- Inline comments highlight key relationships (e.g., `DPA_KT_RUNS_200` env var in `trainer.py`, figure output paths, dataset batch-size heuristics).

## Lesson
- Place a full, up-to-date workspace tree at the top of `AGENTS.md` so new agents don't waste tokens exploring directory structure. Update it when the code layout changes.
