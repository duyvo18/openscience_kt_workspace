# 2026-07-20 — Rename runs/, launch 200-epoch + 5-fold CV sweep

## Goal (today)
1. Rename `code/runs/` → `code/runs-50-epochs/` and update every reference.
2. Re-evaluate `DPA_KT_master.ipynb`; write a sequential training script that
   retrains every dataset for 200 epochs with 5-fold CV into
   `code/runs-200-epochs/`.
3. Materialize the results into two notebooks:
   `notebooks/DPA_KT_200_epochs_ENG.ipynb` and
   `notebooks/DPA_KT_200_epochs_VN.ipynb`.
4. Hardware cap on shared GPU: 35 GB VRAM per the user (then revised to
   10 GB per process after observing only ~11 GB free with vLLM pinned).

## Decisions
- Scope confirmed with user: **full model only**, no new ablation runs
  (re-use the 50-epoch ablation matrix). 7 datasets × 5 folds = 35 runs.
- Output root: `code/runs-200-epochs/` (override via `DPA_KT_RUNS_200`).
- A `DPA_KT_RUNS_200` env var (read in `dpa_kt/training/trainer.py`) routes
  every fold into the new root while leaving 50-epoch runs untouched.
- Sweep is sequential, idempotent (skip when `test_metrics.json` exists),
  and aborts any fold whose peak VRAM exceeds the cap.
- **Batch size per dataset** (from probe_batch.py + heuristics):
    assist09/algebra05 → 192 (measured peak 9.79 / 9.62 GB)
    bridge06/xes3g5m/assist12 → 128
    eedi → 96
    junyi → 64 (largest KC count)
- Cap = 10 GB per process (the vLLM Qwen3.5 process keeps ~67 GB pinned,
  leaving only ~22 GB free; 10 GB per process keeps us safely under the
  shared-RAM ceiling and prevents OOM collisions).

## What changed
- `code/runs/` → `code/runs-50-epochs/` (mv; dir was gitignored, not tracked).
- `code/dpa_kt/config.py`: added `RUNS_DIR_200` (default `runs-200-epochs/`).
- `code/dpa_kt/training/trainer.py`: `_default_run_root()` honors the
  `DPA_KT_RUNS_200` env var.
- `code/scripts/train_200_cv.py`: sequential sweep driver (per-dataset
  batch_size map, 10 GB RAM cap, idempotent via `test_metrics.json`).
- `code/scripts/probe_batch.py`: 1-epoch probe that records peak from the
  trainer's `log.csv` (the GB10's `nvidia-smi memory.used` reports N/A).
- `code/scripts/{train.py,run_all.sh,queue_run.sh,build_notebook.py}` and
  `code/README.md`, `code/notebooks/DPA_KT_master.ipynb`: updated literal
  `runs/` paths to `runs-50-epochs/`.
- `code/scripts/build_notebook_200.py` + notebooks:
  `DPA_KT_200_epochs_ENG.ipynb` and `DPA_KT_200_epochs_VN.ipynb`.

## Sweep status (live)
- `bgp_f82556a75001E4mvHUPacYNb9T` running (persistent).
- assist09_full_fold0: epoch 1 done in 77 s, peak 9.79 GB at b=192
  (3× faster than the original b=64 which took 100 s/epoch).
- 19/35 folds DONE as of 2026-07-22 01:38 UTC; xes3g5m_full_fold4
  in progress (epoch 47/200, val AUC 0.81, 7.17 GB peak).
- mean AUC so far: assist09 0.7128, algebra05 0.7818, bridge06 0.7711,
  xes3g5m (4 folds) 0.8061.

## Figure extraction (waits for sweep completion)
- `code/scripts/extract_figures_200.py` is now watching for
  `sweep_manifest.json` to appear (process `bgp_f878359f0001FeJ8AlPpUdSlfA`).
- Once sweep finishes it will:
  1. generate per-fold curves, CV summary, composite 7×5 grid,
     throughput+memory plots into `code/notebooks/figures-200/`
  2. inline those PNGs as base64 data URIs into
     `notebooks/DPA_KT_200_epochs_ENG.ipynb` and
     `notebooks/DPA_KT_200_epochs_VN.ipynb` as a new
     `## 8. Embedded figures (PNG, generated from sweep)` section.
- Test run on partial data (19/35 folds): generated 23 figures
  (19 per-fold + 4 summaries), embedded successfully into both notebooks
  (each notebook now 2.8 MB; nbformat validated).

## Open items
- Wait for sweep to finish. If any fold exceeds 10 GB peak, halve its
  `BATCH_SIZE[ds]` in `code/scripts/train_200_cv.py` and rerun with `--resume`.
- After sweep completes, the watcher will refresh both ENG + VN notebooks
  with the full set of figures.